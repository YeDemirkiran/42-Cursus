#include "get_next_line.h"

char	*get_next_line(int fd)
{
	if (fd < 0)
		return (NULL);
	return (NULL);
}

#include "stdio.h"
#include "fcntl.h"

int main(void)
{
	printf("Buffer size: %i\n", BUFFER_SIZE);
	return (0);
}
